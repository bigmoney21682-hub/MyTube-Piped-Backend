# The port to listen on.
# The port to listen on.
PORT:8080
# The number of workers to use for the server
HTTP_WORKERS:2

# Proxy (use official ones or leave default)
PROXY_PART:https://pipedproxy-cdg.kavin.rocks

# Public API URL (your Render backend)
API_URL:https://mytube-piped-backend.onrender.com

# Public Frontend URL (your GitHub Pages site)
FRONTEND_URL:https://bigmoney21682-hub.github.io/MyTube-Piped-Frontend

# Disable Registration
DISABLE_REGISTRATION:true

# Feed Retention Time in Days
FEED_RETENTION:30

# Disable CPU expensive timers (fine for single instance)
DISABLE_TIMERS:false

# RYD Proxy URL
RYD_PROXY_URL:https://ryd-proxy.kavin.rocks

# SponsorBlock Servers
SPONSORBLOCK_SERVERS:https://sponsor.ajay.app,https://sponsorblock.kavin.rocks

# Disable the usage of RYD (optional)
DISABLE_RYD:false

# Disable the inclusion of LBRY streams
DISABLE_LBRY:false

# How long should unauthenticated subscriptions last for
SUBSCRIPTIONS_EXPIRY:30

# Send consent accepted cookie
CONSENT_COOKIE:true

# Hibernate properties — MUST use colon, no spaces
hibernate.connection.url:jdbc:postgresql://dpg-d51q7n3uibrs739fk25g-a:5432/mytube_piped_db
hibernate.connection.username:mytube_piped_db_user
hibernate.connection.password:eOVJfcuewJex7TwM4KdLR1yyuymPIEld
hibernate.connection.driver_class:org.postgresql.Driver
hibernate.dialect:org.hibernate.dialect.PostgreSQLDialectPORT:8080
# The number of workers to use for the server
HTTP_WORKERS:2

# Proxy (use official ones or leave default)
PROXY_PART:https://pipedproxy-cdg.kavin.rocks

# Public API URL (your Render backend)
API_URL:https://mytube-piped-backend.onrender.com

# Public Frontend URL (your GitHub Pages site)
FRONTEND_URL:https://bigmoney21682-hub.github.io/MyTube-Piped-Frontend

# Disable Registration
DISABLE_REGISTRATION:true

# Feed Retention Time in Days
FEED_RETENTION:30

# Disable CPU expensive timers (fine for single instance)
DISABLE_TIMERS:false

# RYD Proxy URL
RYD_PROXY_URL:https://ryd-proxy.kavin.rocks

# SponsorBlock Servers
SPONSORBLOCK_SERVERS:https://sponsor.ajay.app,https://sponsorblock.kavin.rocks

# Disable the usage of RYD (optional)
DISABLE_RYD:false

# Disable the inclusion of LBRY streams
DISABLE_LBRY:false

# How long should unauthenticated subscriptions last for
SUBSCRIPTIONS_EXPIRY:30

# Send consent accepted cookie
CONSENT_COOKIE:true

# Hibernate properties — MUST use colon, no spaces
hibernate.connection.url:jdbc:postgresql://dpg-d51q7n3uibrs739fk25g-a:5432/mytube_piped_db
hibernate.connection.username:mytube_piped_db_user
hibernate.connection.password:eOVJfcuewJex7TwM4KdLR1yyuymPIEld
hibernate.connection.driver_class:org.postgresql.Driver
hibernate.dialect:org.hibernate.dialect.PostgreSQLDialect
